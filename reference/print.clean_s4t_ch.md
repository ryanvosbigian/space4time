# Print summary of `clean_s4t_ch` object

Returns the observations of individuals that had observations dropped
for data cleaning.

## Usage

``` r
# S3 method for class 'clean_s4t_ch'
print(x, ...)
```

## Arguments

- x:

  `clean_s4t_ch` object from `clean_s4t_ch()`

- ...:

  passed to `print.tibble()`
